apiVersion: v1
kind: Service
metadata:
  name: docker-registry
  namespace: operator-controller-e2e
spec:
  selector:
    app: registry
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 30000
  type: NodePort
