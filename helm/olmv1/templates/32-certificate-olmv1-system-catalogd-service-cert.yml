{{- if .Values.components.certManager.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  annotations:
    {{- include "olmv1.annotations" . | nindent 4}}
  name: catalogd-service-cert
  namespace: {{ .Values.namespaces.olmv1 }}
spec:
  dnsNames:
    - localhost
    - catalogd-service.{{ .Values.namespaces.olmv1 }}.svc
    - catalogd-service.{{ .Values.namespaces.olmv1 }}.svc.cluster.local
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: olmv1-ca
  privateKey:
    algorithm: ECDSA
    size: 256
  secretName: catalogd-service-cert-git-version
{{- end }}
